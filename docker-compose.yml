networks:
  demo:
    driver: bridge
    external: true
services:
  migrate:
    build: .
    command: npx sequelize-cli db:migrate --config dist/config/config.json --env development 
    container_name: migrate-cat
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - demo
    environment:
      - -NODE_ENV=production
    env_file:
      - .env.production
  app:
    build: .
    container_name: cat
    ports: 
      - "3003:3004"
    environment:
      - NODE_ENV=production
      - REDIS_HOST=redis
    env_file:
      - .env.production
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    depends_on:
      - postgres
      - redis
    networks:
      - demo
 
  postgres:
    image: postgres:17
    networks:
      - demo
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: sangduong
      POSTGRES_DB: cat
      POSTGRES_PASSWORD: 210919
    volumes: 
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sangduong -d cat"]
      interval: 10s
      timeout: 5s
      retries: 5


  redis:
    image: redis/redis-stack
    container_name: redis
    networks:
      - demo
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - redis_data:/data

  
volumes:
  postgres_data: null 
  pgadmin_data: null
  redis_data: null

