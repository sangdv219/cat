# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Build Image Development CI/CD

on:
  push:
    branches: [ "main", "staging", "develop", "feat/**" ]
  workflow_dispatch:
    # logLevel: 'info'
  # pull_request:
  #   branches: [ "main", "staging" ] # production, staging

jobs:
  # lint:
  #   if: github.ref != 'refs/heads/main' && github.ref != 'refs/heads/staging'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Run ESLint
  #       run: npm run lint

  # test:
  #   if: github.ref != 'refs/heads/main' && github.ref != 'refs/heads/staging'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Run Tests
  #       run: npm run test --if-present

  # sonarQube:
  #   if: github.ref != 'refs/heads/main' && github.ref != 'refs/heads/staging'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci
        
  #     - name: SonarQube Scan 
  #       env:
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  #       run: npm run sonar 

  staging:
    if: github.ref == 'refs/heads/staging'
    runs-on: [self-hosted, macOS]
    environment:
      name: staging  
      url: ${{vars.URL}}   
    env:
      IMG_NAME: cat-app 
      USERNAME: sangdev2109
      PF_ENV: ${{vars.PF_ENV}}
    permissions:
      contents: read
      packages: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        clean: true
    - name: Log in to Hub Docker 
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_TOKEN }}
    - name: Build docker image save version tag
      run: |
        docker build -f Dockerfile  --build-arg PF_ENV="${PF_ENV}" -t ${{ env.IMG_NAME }} .
        docker tag ${{ env.IMG_NAME }}:latest ${{ env.USERNAME }}/${{ env.IMG_NAME }}:latest
    - name: Push docker image lastest and version tag
      run: |
        docker push ${{ env.USERNAME }}/${{ env.IMG_NAME }}:latest
  deploy-staging:
    if: github.ref == 'refs/heads/staging'
    needs: staging
    runs-on:  ubuntu-latest
    environment:
      name: staging
      url: ${{vars.URL}}
    steps:
    - name: Execute Deployment in Staging
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.STAGING_SERVER_HOST }}
        username: ${{ secrets.STAGING_SERVER_USERNAME }} 
        port: ${{ secrets.STAGING_SERVER_SSH_PORT || 22 }}
        key: ${{ secrets.STAGING_SERVER_SSH_KEY }}
        debug: true
        envs: APP_NAME, DOCKER_HUB_TOKEN, STAGING_DB_HOST, STAGING_DB_PORT, STAGING_DB_USER, STAGING_DB_PASSWORD, STAGING_DB_DATABASE, STAGING_REDIS_HOST
        script: |
          echo "---------- Start deploy-staging.sh ----------"
          # Ví dụ lệnh pull image mới về
          docker pull ${{ env.USERNAME }}/${{ env.IMG_NAME }}:latest
          # Stop và xóa container cũ, sau đó chạy container mới...
          echo "Connection successful!"
          echo "---------- Finished deploy-staging.sh ----------"
      env:  
          APP_NAME: cat
          DOCKER_HUB_TOKEN: ${{ secrets.DOCKER_HUB_TOKEN }}
          STAGING_DB_HOST: ${{ secrets.STAGING_DB_HOST }}
          STAGING_DB_PORT: ${{ secrets.STAGING_DB_PORT }}
          STAGING_DB_USER: ${{ secrets.STAGING_DB_USER }}
          STAGING_DB_PASSWORD: ${{ secrets.STAGING_DB_PASSWORD }}
          STAGING_DB_DATABASE: ${{ secrets.STAGING_DB_DATABASE }}
          STAGING_REDIS_HOST: ${{ secrets.STAGING_REDIS_HOST }}
  
  deploy-prod:
    if: github.ref == 'refs/heads/main'
    runs-on:  ubuntu-latest
    environment:
      name: production
      url: ${{vars.URL}}
    steps:
    - name: Deploy to EC2 via SSH in Production
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ubuntu 
        key: ${{ secrets.EC2_SSH_KEY }}
        envs: APP_NAME
        script: |
          echo "---------- Start deploy-prod.sh on EC2 ----------"

          bash ${{vars.PATH_DEPLOY}}
          echo "---------- Finished deploy-prod.sh ----------"
      env:
        APP_NAME: cat