# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: 
      - develop # staging
      - master  # production
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [20.x, 22.x]
        os: [ubuntu-latest] # or windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: 'npm'
      - run: npm install
      - run: npm test 
      - run: npm run build 
      - name: SonarQube Scan
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: npm run sonar 
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-build
          path: dist/



  deploy-staging:
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    needs: build 
    environment:
      name: staging
      url: https://notebooklm.google.com/notebook/8fc114df-f7ca-4af4-81ff-4886294e8ff4    #util
    steps:
        - uses: actions/checkout@v4
        - name: Download build artifact
          uses: actions/download-artifact@v4
          with:
            name: app-build
        - name: Check STAGING database variable
          run: | 
            echo "View variable STAGING database"
              if [ -z "${{vars.DATABASE_URL}}" ]; then
                  echo "❌ DATABASE_URL is empty"
              else
                echo "✅ DATABASE_URL is set: ${{vars.DATABASE_URL}}"
              fi
      
        - name: Deploy
          run: echo "Deploy to STAGING server"
      # - run: ./scripts/deploy-staging.sh     #deploy



  deploy-prod:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    needs: build    # đảm bảo,chắc chắn build pass mới deploy
    environment:
      name: production
      url: https://notebooklm.google.com/notebook/8fc114df-f7ca-4af4-81ff-4886294e8ff4    #util
    # steps:
    #   - run: ./scripts/deploy-prod.sh

    steps:
    - uses: actions/checkout@v4
    - name: Download build artifact
      uses: actions/download-artifact@v4

    - name: Use Node.js 22.x
      with:
          name: app-build 
    - name: Deploy
      run: echo "Deploy to PRODUCTION server"
   

          
    # - name: Build & push Docker image
    #   uses: docker/build-push-action@v6
    #   with:
    #     push: true
    #     tags: ghcr.io/sangduong-dev/nest-postgres:latest
    #   run: docker build -t cat .

    # - name: Notify
    #   uses: appleboy/telegram-action@master
    #   with:
    #     to: ${{ secrets.TG_CHAT_ID }}
    #     token: ${{ secrets.TG_BOT_TOKEN }}
    #     message: "🚨 Deploy failed at ${{ github.sha }}"
          

